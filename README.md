 include both TCP and UDP variation in your application. 
 Hint: May be, you write a server application that tracks the number of clients connected to server.
 Each client will create few villains and then pass it to Server. 
 The server then create the corresponding heroes and pass the heroes objects to each connected client.
